/**
 * Enum for Token Type code
 * @readonly
 * @example TokenTypeEnum.ACCESS_TOKEN
 * @returns {string} Returns the value of none
 */
export enum TokenTypeEnum {
  ACCESS_TOKEN = 'accessToken',
  REFRESH_TOKEN = 'refreshToken',
}
